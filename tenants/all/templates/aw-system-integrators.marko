import { get } from "@parameter1/base-cms-object-path";

$ const { newsletter, date } = data;
$ const { config } = out.global;

$ const newsletterConfig = config.get(newsletter.alias);
$ const logoConfig = get(newsletterConfig, "logoConfig");

<common-standard-layout
  data=data
  style-component="standard"
  style-component-input={ "accent-color": "#5b2333" }
  sections=["Feature", "Standard", "What's Trending"]
>
  <@header>
    <common-standard-header-block
      newsletter=newsletter
      date=date
      ...logoConfig
      image-wrapper-style={ "padding": "16px 10px 16px 10px" }
      image-attrs={ width: 256, height: 33 }
      image-style={ "max-width": "256px" }
      image-options={ w: 256 }
      image-src="/files/base/pmmi/all/image/newsletters/aw-system-integrators-header.png"
      secondary-background-color="#5b2333"
    />
  </@header>
</common-standard-layout>
